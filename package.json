{
  "name": "kamal",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ kamal",
    "create-local": "graph create --node http://localhost:8020/ kamal",
    "remove-local": "graph remove --node http://localhost:8020/ kamal",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kamal"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
